.icon {
  display: inline-block;
  width: $rem(30px);
  height: $rem(30px);
  background-color: $white;
  border-radius: $rem(30px);
  margin-right: $rem(5px);

  svg {
    width: 100%;
    height: 100%;
    fill: $highlight;
  }

  .button & {
    position: relative;
    top: $rem(6px);
  }
}
